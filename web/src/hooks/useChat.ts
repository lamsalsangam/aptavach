// Superseded by useChats.ts (per-project, multi-chat). Kept only as a type re-export so any
// lingering imports resolve; safe to delete once nothing references it.
export type { ChatMessage } from '@/lib/types'
